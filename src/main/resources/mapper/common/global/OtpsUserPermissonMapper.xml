<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.octopusmall.common.global.mapper.OtpsUserPermissionMapper">

    <select id="selectUserValidPermissionList" resultType="string">
        SELECT
            p.permission_name
        FROM otps_user_permission up
                 INNER JOIN otps_permission p ON up.permission_name = p.permission_name
        WHERE up.user_id = #{userId}
          AND (up.expire_time IS NULL OR up.expire_time > SYSTIMESTAMP)
    </select>

    <insert id="batchInsertUserPermissions">
        INSERT ALL
        <foreach collection="permissions" item="item" separator=" ">
            INTO otps_user_permission (
                id,
                user_id,
                permission_name,
                create_time,
                expire_time
            ) VALUES (
                #{item.id},
                #{item.userId},
                #{item.permissionName},
                #{item.createTime},
                #{item.expireTime}
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <delete id="batchDeleteUserPermissions">
        DELETE FROM otps_user_permission
        WHERE user_id = #{userId}
          AND permission_name IN (
            <foreach collection="permissionNames" item="item" separator=",">
                #{item}
            </foreach>
          )
    </delete>

    <select id="selectUserPermission" resultType="OtpsUserPermission">
        SELECT
            id,
            user_id,
            permission_name,
            create_time,
            expire_time
        FROM otps_user_permission
        WHERE user_id = #{userId}
          AND permission_name = #{permissionName}
    </select>

    <select id="selectExistPermissionNames" resultType="string">
        SELECT permission_name
        FROM otps_user_permission
        WHERE user_id = #{userId}
          AND permission_name IN (
            <foreach collection="permissionNames" item="item" separator=",">
                #{item}
            </foreach>
          )
    </select>

</mapper>
